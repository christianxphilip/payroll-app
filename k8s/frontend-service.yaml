apiVersion: v1
kind: Service
metadata:
  name: payroll-frontend
  labels:
    app: payroll-frontend
spec:
  type: LoadBalancer
  ports:
  - port: 5174
    targetPort: 5173
    protocol: TCP
  selector:
    app: payroll-frontend
